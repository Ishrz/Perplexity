// Socket.io implementation removed
// Using REST API only via useChatOperations hook
export const useChatSocket = () => {
    // No-op hook for backward compatibility
    return null;
};

export const handelChatSocket = () => {
    // No-op for backward compatibility
    return () => null;
};
